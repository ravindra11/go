The go build command, compiles the packages, along with their dependencies, but it doesn't install the results.
    ---------

The go install command, compiles and install the packages.
    ----------

    go build =====> to compile the code into an executable. Will create an .exe executable application.


